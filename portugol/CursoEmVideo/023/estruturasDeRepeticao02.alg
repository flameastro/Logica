algoritmo "estruturasDeRepeticao02"
var contador: inteiro

inicio
      // Praticando o repita
      contador <- 10

      Repita
            Escreval(contador)
            contador <- contador - 1
      Ate (contador <= 0)
fimalgoritmo
