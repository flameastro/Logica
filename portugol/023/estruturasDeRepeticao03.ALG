algoritmo "estruturasDeRepeticao03"
var contador: inteiro

inicio
      contador <- 1

      repita
            Escreval(contador)
            contador <- contador + 1)
      Ate (contador > 10)
fimalgoritmo
